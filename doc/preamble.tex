\usepackage[utf8]{inputenc}
\usepackage[russian]{babel}

\usepackage[left=3cm,right=1.5cm,top=2cm,bottom=1.6cm]{geometry} % Поля
\usepackage{setspace}
\linespread{1.25} % Полуторный межстрочный интервал

%\usepackage{parskip} % Настройка интервалов между абзацами
% \setlength{\parskip}{6pt} % Размер интервалов -- по умолчанию вроде норм, но не по ГОСТу (по ГОСТу интервала нет) 

\usepackage{indentfirst} % Красная строка

\setlength{\parindent}{1.25cm} % Размер красной сроки

\usepackage{enumitem} % Настройка списков
\setlist{nosep} % Нет интервалов между пунктами и вертикальных интервалов

\setlist[enumerate]{leftmargin=*, label=\arabic*)} % Чтобы работали отступы, ) вместо .
\setlist[itemize]{leftmargin=*} % Чтобы работали отступы
\setlist[1]{labelindent = \parindent} % Абзацный отступ для списокв


\frenchspacing % Расстояние между предложениями такое же, как между словами

\usepackage[normalem]{ulem} % Нижнее подчеркивание (на титульнике)

\usepackage{titlesec} % Настройка заголовков
%\titlespacing*{\chapter}{\parindent}{*4}{*2} % Отступы до и после заголовков
%\titlespacing*{\section}{\parindent}{*4}{*2} % По ГОСТу - одна пустая строка (вроде)
%\titlespacing*{\subsection}{\parindent}{*4}{*2} 
%\titleformat{\section}{\normalsize\bfseries}{0}{}{} % Начиная с {0} - хз, работает

\titlespacing*{\chapter}{0pt}{-30pt}{8pt}
\titlespacing*{\section}{\parindent}{*4}{*2}
\titlespacing*{\subsection}{\parindent}{*4}{*2}
\titleformat{\chapter}{\LARGE\bfseries}{\thechapter}{20pt}{\LARGE\bfseries}
\titleformat{\section}{\Large\bfseries}{\thesection}{20pt}{\Large\bfseries}

\usepackage{pgfplots} % Вообще - для всяких графиков, но на титульнике с помощью этой штуки логотип МГТУ выводится
\usepackage{graphics} % Вставка картинок

\usepackage{listings} % Настройка листингов 
\definecolor{comments}{rgb}{0.36, 0.54, 0.66} % Кастомный стиль (цвет комментариев)
\lstdefinestyle{customgo}{
	language=Go,
	commentstyle=\color{comments}
}
\lstset{ 
	language=Go, % Язык 
	frame=single, % Рамка
	breaklines=true, % Перенос строки
	style=customgo
	%xleftmargin=-1.7cm % На глаз - чтобы не было отступа слева
}

\usepackage{caption} % Настройка подисей к листингам
\captionsetup[lstlisting]{labelsep = endash, singlelinecheck = false, justification = raggedright}

\usepackage{tabto} % Для отступов в диагарамах 

\addto{\captionsrussian}{\renewcommand*{\contentsname}{Содержание}} % Содержание вместо оглавления

\bibliographystyle{utf8gost705u}  % стилевой файл для оформления по ГОСТу

\usepackage{pdfpages} % Вставка PDF

\usepackage{scrextend}